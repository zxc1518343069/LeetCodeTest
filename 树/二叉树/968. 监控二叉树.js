/**
 * Definition for a binary tree node.
 * function TreeNode(val) {
 *     this.val = val;
 *     this.left = this.right = null;
 * }
 */
/**
 * @param {TreeNode} root
 * @return {number}
 */
let minCameraCover = function(root) {
  let res = 0
  if (root === null) return res
  let dfs = (root)=>{
    if (root === null) return res



  }
  return res
};

